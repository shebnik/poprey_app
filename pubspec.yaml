name: poprey_app
description: Poprey.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  html: ^0.15.0
  intl: ^0.17.0
  shared_preferences: ^2.0.8
  get: ^4.3.8
  google_fonts: ^2.1.0
  connectivity: ^3.0.6
  provider: ^6.0.1
  flutter_svg: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  lints: ^1.0.1

flutter:

  uses-material-design: true

  assets:
    - lib/assets/images/instagram.svg
    - lib/assets/images/additional_services.svg
import 'package:flutter/material.dart';

class AutoLikesSheet extends StatelessWidget {
  const AutoLikesSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Wrap(
        alignment: WrapAlignment.center,
        children: const [],
      ),
    );
  }
}
